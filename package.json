{
  "name": "athenas-pallas",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "pnpm dev",
    "bootstrap": "node scripts/bootstrap.mjs",
    "build": "pnpm --dir pallas-app run tauri build",
    "build:engine": "cargo build --release -p athenas-pallas"
  }
}
