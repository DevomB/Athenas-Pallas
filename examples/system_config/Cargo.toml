[package]
name = "system_config"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
athenas-pallas = { path = "../../athenas-pallas" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
