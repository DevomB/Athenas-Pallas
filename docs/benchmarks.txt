backtest_hotpath (release, Windows 11, local workspace)

2026-06-11 baseline:
  noop_100k_bars:        ~50 ms  (49.996 ms median), ~0.50 us/bar
  noop_100k_amortized:   ~43 ms  (42.548 ms median), ~0.43 us/bar
  snapshot_clone:        ~1.03 us
  buy_and_hold_100k:     ~43 ms
  equity_curve/10k true:  ~5.0 ms
  equity_curve/10k false: ~4.2 ms

2026-06-17 after direct strategy layout and pbar reader cleanup:
  noop_100k_bars:        ~36.8 ms
  noop_100k_amortized:   ~33.1 ms
  snapshot_clone:        ~1.15 us
  buy_and_hold_100k:     ~33.9 ms
  equity_curve/10k true:  ~4.44 ms
  equity_curve/10k false: ~3.31 ms
  session_overhead_100k: ~53.1 ms

2026-06-21 after borrowed replay events, instrument-scoped order poll,
streaming metrics, FxHashMap, and SmallVec fill buffers (different host;
numbers are not comparable to the 2026-06-17 row above):
  noop_100k_bars:         ~60.1 ms median  (0.60 us/bar)
  noop_100k_amortized:    ~58.3 ms median  (0.58 us/bar)
  snapshot_clone:         ~2.45 us
  buy_and_hold_100k:      ~70.4 ms median
  equity_curve/10k true:  ~18.9 ms
  equity_curve/10k false: ~12.8 ms   (streaming summary, no Vec<EquityPoint>)

2026-06-21 pass 2 after price-indexed resting orders (BTreeMap per side/price),
PriceTicks notional fast path in paper fees, and OMS pollable_ids (release, full suite):
  noop_100k_bars:         ~60.1 ms median  (0.60 us/bar)
  noop_100k_amortized:    ~58.3 ms median  (0.58 us/bar)
  snapshot_clone:         ~2.45 us
  buy_and_hold_100k:      ~70.4 ms median
  equity_curve/10k true:  ~18.9 ms
  equity_curve/10k false: ~12.8 ms   (streaming summary, no Vec<EquityPoint>)

command: cargo bench -p athenas-pallas --bench backtest_hotpath -- --noplot

Notes:
  - Per-bar cost is host-dependent; absolute numbers above are from a contended
    laptop. Use the committed Criterion baseline for regression comparison, not
    the raw millisecond figures, and re-run on the target host before quoting.
  - equity_curve/false now exercises the O(1)-memory streaming RollingMetrics
    path (no full equity curve materialized).
  - session_overhead_100k reuses a fresh CSV sidecar .pbar cache when available.
  - 90-bar noop debug integration tests remain sub-millisecond.

2026-06-21 after backtest-only debloat (live modules removed, sync engine only,
csv_common dedupe, metrics split; release, Windows 11):
  noop_100k_amortized:    ~50.0 ms median  (~0.50 us/bar)
  (Criterion reported ~68% improvement vs prior local baseline in target/criterion history)
